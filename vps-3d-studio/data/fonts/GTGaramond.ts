
import { createMetrics } from './utils';

export const Regular = createMetrics(0.70);
export const Bold = createMetrics(0.75);
export const Italic = createMetrics(0.68);
export const BoldItalic = createMetrics(0.72);
export const Book = createMetrics(0.70);

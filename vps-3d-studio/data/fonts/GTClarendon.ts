import { createMetrics } from './utils';

export const Medium = createMetrics(0.82);
export const FortuneBold = createMetrics(0.88);
export const CrawCondensed = createMetrics(0.65);
export const ClarionMedium = createMetrics(0.80);
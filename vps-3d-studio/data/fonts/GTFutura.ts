
import { createMetrics } from './utils';

export const Regular = createMetrics(0.80);
export const Bold = createMetrics(0.85);
export const Condensed = createMetrics(0.60);
export const ExtraBoldItalic = createMetrics(0.90);
export const Demibold = createMetrics(0.82);
export const BoldFormed = createMetrics(0.88);
